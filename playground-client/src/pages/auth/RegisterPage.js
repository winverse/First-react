import React, { Fragment } from 'react';

import {
  HeaderContainer,
  RegisterContainer
} from 'containers';

const RegisterPage = () => (
  <Fragment>
    <RegisterContainer/>
  </Fragment>
);

export default RegisterPage;